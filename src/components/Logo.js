import React from 'react'

const Logo = ()=>(
     <header style={{ marginBottom: `12vh`,textAlign:`center` }}>
                <h1 style={{ letterSpacing: `-2px`, marginBottom: `0`, fontSize: `6rem`, lineHeight: `5rem` }}>LSM</h1>
                <h4 className="f-serif">MAGLIFICIO</h4>
            </header>
)
 export default Logo